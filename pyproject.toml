[project]
name = "agent-memory"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
cognee = ["cognee"]

[project.scripts]
agent-integrate = "integrate:main"
